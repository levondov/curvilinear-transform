#
# Levon Dovlatyan (levondov@berkeley.edu)
# Danielle Christianson
# Coordinate Transformations
# 9 May 2014
#




import numpy as np
from coord_transform import coord_transform
from ctGUI import ctGUI

ctGUI = ctGUI()

#centerlineX,centerlineY = cd.getCenterline() #get the centerline points, WARNING: they are very large arrays
#st = cd.getst()
#cd.transform(st[:,0].astype(np.float),st[:,1].astype(np.float))
#s,n,l = cd.transform(xdata,ydata)
#fig = plt.figure()
#ax = fig.add_subplot(111, projection='3d')
#ax.scatter(xdata,ydata,zdata)

#fig1 = plt.figure()
#plt.grid(True)
#ax = fig1.add_subplot(111, projection='3d')
#ax.scatter(s*l,n,zdata)
#plt.show()
